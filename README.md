# Installation

Clone into a subdirectory of `$HOME` (e.g. `~/dotfiles`). Then:

```console
~$ cd ~/dotfiles
~/dotfiles$ stow vim
~/dotfiles$ stow kitty
```

etc.

# Currently versioned configs

- vim
- kitty

